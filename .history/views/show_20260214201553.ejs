<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Posts - ABD AJ</title>

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Chart.js CDN (agar future mein chart chahiye to) -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <style>
    body {
      background-color: #0f0f0f;
      color: #e5e5e5;
    }
    .sidebar-active {
      background-color: #1a1a1a;
      border-left: 4px solid #ef4444;
    }
    .card-hover:hover {
      transform: translateY(-4px);
      box-shadow: 0 10px 20px rgba(239, 68, 68, 0.15);
      border-color: #ef4444;
    }
  </style>
</head>
<body class="min-h-screen font-sans antialiased">

  <div class="flex h-screen overflow-hidden">

    <!-- Sidebar - Black & Red Theme -->
    <aside class="hidden md:block w-64 bg-black text-white flex flex-col border-r border-gray-800">
      <div class="p-6 border-b border-gray-800">
        <h1 class="text-2xl font-bold text-red-500">ABD AJ</h1>
      </div>
      
      <nav class="flex-1 px-4 py-6 space-y-2">
        <a href="/" class="flex items-center px-4 py-3 rounded-lg text-gray-400 hover:bg-gray-900 transition">
          <span class="mr-3 text-xl">🏠</span> Home
        </a>
        <a href="/api" class="flex items-center px-4 py-3 rounded-lg sidebar-active">
          <span class="mr-3 text-xl">📝</span> Posts
        </a>
        <a href="/api/new" class="flex items-center px-4 py-3 text-gray-400 hover:bg-gray-900 rounded-lg transition">
          <span class="mr-3 text-xl">➕</span> Add New
        </a>
        <a href="#" class="flex items-center px-4 py-3 text-gray-400 hover:bg-gray-900 rounded-lg transition">
          <span class="mr-3 text-xl">⚙️</span> Settings
        </a>
        <a href="#" class="flex items-center px-4 py-3 text-gray-400 hover:bg-gray-900 rounded-lg transition">
          <span class="mr-3 text-xl">🚪</span> Logout
        </a>
      </nav>
    </aside>

    <!-- Main Content Area -->
    <div class="flex-1 flex flex-col overflow-hidden">

      <!-- Top Navbar -->
      <header class="bg-black border-b border-gray-800 px-6 py-4 flex items-center justify-between">
        <h2 class="text-2xl font-bold text-white">All Posts</h2>
        
        <div class="flex items-center gap-6">
          <a href="/api/new" class="px-5 py-2.5 bg-red-600 hover:bg-red-700 text-white rounded-lg font-medium transition shadow-md">
            + Add New Post
          </a>
          
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-gray-700 flex items-center justify-center text-white font-bold">
              👤
            </div>
            <div class="hidden sm:block">
              <p class="font-medium text-sm">
                <%= currentUser ? (currentUser.username || currentUser.email || 'User') : 'Guest' %>
              </p>
              <p class="text-xs text-gray-500">
                <%= currentUser ? 'Logged In' : 'Not Logged In' %>
              </p>
            </div>
          </div>
        </div>
      </header>

      <!-- Main Content -->
      <main class="flex-1 p-6 overflow-y-auto bg-gray-950">

        <!-- Debug Info -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl p-4 mb-6 text-sm">
          <strong>Debug Info:</strong><br>
          Logged in as: <strong><%= currentUser ? (currentUser.username || currentUser.email || 'No name') : 'Not logged in' %></strong><br>
          User ID: <strong><%= currentUser ? currentUser._id : 'No ID' %></strong>
        </div>

        <!-- Flash Messages (agar partials/flash.ejs hai to include kar lo) -->
        <%- include("partials/flash.ejs") %>

        <!-- Posts Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          <% if (alldata && alldata.length > 0) { %>
            <% alldata.forEach(data => { %>
              <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden shadow-lg card-hover transition-all duration-300">
                <!-- Image -->
                <% if (data.img?.url) { %>
                  <img src="<%= data.img.url %>" class="w-full h-48 object-cover" alt="<%= data.name || 'Post image' %>">
                <% } else { %>
                  <div class="w-full h-48 bg-gray-800 flex items-center justify-center text-gray-500">
                    No Image
                  </div>
                <% } %>

                <!-- Content -->
                <div class="p-5">
                  <h5 class="text-lg font-semibold text-white mb-3 line-clamp-2">
                    <%= data.name || 'Untitled Post' %>
                  </h5>

                  <div class="text-sm text-gray-400 space-y-2">
                    <p><strong>Degree:</strong> <%= data.deg || '—' %></p>
                    <p><strong>Age:</strong> <%= data.age || '—' %></p>
                    <p class="text-red-400 font-medium">
                      Posted by @<%= data.owner?.username || data.owner?.email || 'anonymous' %>
                    </p>
                  </div>

                  <!-- Owner Actions -->
                  <% if (currentUser && data.owner && currentUser._id?.toString() === data.owner._id?.toString()) { %>
                    <div class="mt-6 flex gap-3">
                      <a href="/api/update/<%= data._id %>"
                         class="flex-1 text-center py-2 bg-yellow-600 hover:bg-yellow-700 text-white rounded-lg transition text-sm font-medium">
                        Update
                      </a>
                      <form action="/api/delete/<%= data._id %>?_method=DELETE" method="POST" class="flex-1">
                        <button type="submit"
                                onclick="return confirm('Are you sure you want to delete this post?')"
                                class="w-full py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg transition text-sm font-medium">
                          Delete
                        </button>
                      </form>
                    </div>
                  <% } else { %>
                    <div class="mt-6 text-center text-sm text-gray-500 italic">
                      View Only
                    </div>
                  <% } %>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <div class="col-span-full text-center py-16">
              <div class="text-6xl mb-4">📪</div>
              <h3 class="text-2xl font-semibold text-gray-300 mb-3">No posts yet</h3>
              <p class="text-gray-500 mb-6">Be the first to share something awesome!</p>
              <a href="/api/new" class="inline-block bg-red-600 text-white px-8 py-3 rounded-xl font-medium hover:bg-red-700 transition shadow-lg">
                + Add First Post
              </a>
            </div>
          <% } %>
        </div>

      </main>
    </div>
  </div>

</body>
</html>